/**
 * Прайс платных услуг — в одном месте.
 *
 * Цены лежат здесь, а не в словарях: словари переводятся, а сумма во всех
 * трёх языках одна. Названия и описания услуг — в разделах promo и promoPage
 * словарей.
 */
export const PROMO_PRICES = {
  top: 1000,
  highlight: 2000,
  urgent: 700,
} as const;

export type PromoKind = keyof typeof PROMO_PRICES;

/** Порядок в окне: от самой заметной услуги к самой простой. */
export const PROMO_KINDS: PromoKind[] = ["top", "highlight", "urgent"];

/**
 * Сколько объявлений один человек размещает бесплатно.
 *
 * По правилам — одно. На время бета-тестирования объявлена акция: три.
 * Площадка нужна покупателю только тогда, когда на ней есть из чего выбирать,
 * а выбирать пока не из чего — первых продавцов приходится звать своими силами.
 *
 * Базовое число не забыто, а записано рядом: акция кончится, и правило
 * вернётся. Чтобы её свернуть, достаточно поставить BETA_PROMO в false —
 * и счёт, и тексты на сайте перестанут её обещать сами.
 */
export const FREE_LISTINGS_BASE = 1;

/** Сколько бесплатных даёт акция на время бета-тестирования. */
export const FREE_LISTINGS_PROMO = 3;

/** Идёт ли акция. Один переключатель на весь сайт. */
export const BETA_PROMO = true;

/** Сколько бесплатных действует сейчас. */
export const FREE_LISTINGS = BETA_PROMO ? FREE_LISTINGS_PROMO : FREE_LISTINGS_BASE;

/** Плата за каждое объявление сверх бесплатных. */
export const EXTRA_LISTING_PRICE = 500;

/** Срок действия продвижения в днях. */
export const PROMO_DAYS = 30;

/**
 * Месячные подписки.
 *
 * «Магазин» снимает доплату за объявления сверх бесплатных: limit — сколько
 * объявлений разрешено держать. «Охотник» к объявлениям не относится вовсе:
 * это уведомление о появлении нужной комбинации.
 *
 * У верхнего тарифа стоит потолок в тысячу номеров, а не «без ограничения»:
 * обещать безграничное значит обещать то, чего никто не считал.
 */
export const SUBSCRIPTIONS = [
  { plan: "shop_50", price: 10000, limit: 50 },
  { plan: "shop_100", price: 15000, limit: 100 },
  { plan: "shop_1000", price: 100000, limit: 1000 },
] as const;

export const HUNTER_PRICE = 1000;
